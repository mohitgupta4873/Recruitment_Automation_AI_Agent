# ── Python & Django ──────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
db.sqlite3
db.sqlite3-journal
local_settings.py
staticfiles/
logs/
*.log

# ── Virtual environments ─────────────────────────────────────
venv/
.venv/
env/
ENV/

# ── Secrets & credentials (CRITICAL) ─────────────────────────
# Never commit these. Production reads the same values from real env vars.
.env
.env.*
!.env.example
token.json
client_secrets.json
credentials.json
*.pem
*.key

# ── Candidate data (PII) ─────────────────────────────────────
# Resumes and campaign state contain personal data belonging to applicants.
media/
campaigns/
campaign_state.json
cv_manifest.json
shortlist.json
*.pdf

# ── Local scratch ────────────────────────────────────────────
generate_token.py
check_model.py
check_models.py

# ── Local planning docs (not for the public repo) ────────────
ROADMAP.md

# ── IDE & OS ─────────────────────────────────────────────────
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
